import { Boldonse, Inter_Tight } from 'next/font/google'

export const interTight = Inter_Tight({
  variable: '--font-inter-tight',
  subsets: ['latin'],
})

export const boldonse = Boldonse({
  variable: '--font-boldonse',
  weight: '400',
  subsets: ['latin'],
})
